#include <iostream>
#include <string>
using namespace std;
int main() 
{
string n = "preetham$$";
int c = 0;
string s = "[@_!#$%^&*()<>?}{~:]";
for(int i = 0; i < n.size(); i++)
{
	if (s.find(n[i]) != string::npos )
	{
	c++;
	}
}
if(c)
{
	cout << "string is not accepted" ;
}
else{
	cout << "string is accepted";
}	
	return 0;
}
